---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: thresholdsecrets.threshold.shamir.io
spec:
  group: threshold.shamir.io
  names:
    kind: ThresholdSecret
    listKind: ThresholdSecretList
    plural: thresholdsecrets
    singular: thresholdsecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.threshold
      name: Threshold
      type: integer
    - jsonPath: .spec.shares
      name: Shares
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ThresholdSecret is the Schema for the thresholdsecrets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ThresholdSecretSpec defines the desired state of ThresholdSecret.
            properties:
              scheme:
                default: feldman
                description: Scheme is the secret sharing scheme to use.
                enum:
                - sss
                - feldman
                - pedersen
                type: string
              secretRef:
                description: SecretRef references the Kubernetes Secret to protect.
                properties:
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - name
                type: object
              shares:
                description: Shares is the total number of shares to generate (n).
                minimum: 2
                type: integer
              threshold:
                description: Threshold is the minimum number of shares required to
                  reconstruct the secret (k).
                minimum: 2
                type: integer
            required:
            - secretRef
            - shares
            - threshold
            type: object
          status:
            description: ThresholdSecretStatus defines the observed state of ThresholdSecret.
            properties:
              phase:
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Splitting
                - Active
                - Refreshing
                - Degraded
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
