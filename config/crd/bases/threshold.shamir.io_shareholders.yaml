---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: shareholders.threshold.shamir.io
spec:
  group: threshold.shamir.io
  names:
    kind: ShareHolder
    listKind: ShareHolderList
    plural: shareholders
    singular: shareholder
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.sharesHeld
      name: Shares
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ShareHolder is the Schema for the shareholders API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ShareHolderSpec defines the desired state of ShareHolder.
            properties:
              type:
                description: Type identifies the kind of entity holding shares.
                enum:
                - Node
                - Pod
                - External
                type: string
            required:
            - type
            type: object
          status:
            description: ShareHolderStatus defines the observed state of ShareHolder.
            properties:
              ready:
                description: Ready indicates the holder can participate in share operations.
                type: boolean
              sharesHeld:
                description: SharesHeld is the number of shares currently stored by
                  this holder.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
